import type { ModuleAOutput } from "../types";

export const moduleAMockData: ModuleAOutput = {
  workflowId: "module-a-mock",
  success: true,
  normalizedItems: ["mock customer", "mock invoice"],
};
