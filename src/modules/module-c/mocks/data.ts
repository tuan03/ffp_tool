import type { ModuleCOutput } from "../types";

export const moduleCMockData: ModuleCOutput = {
  workflowId: "module-c-mock",
  success: true,
  processedItems: ["mock report generated", "mock notification queued"],
};
