# mock | development | production
VITE_APP_ENV=development

# --- Amazon Crawler Configuration ---
# Optional: omit this value to use the browser hostname with coordinator port 8766.
# VITE_AMAZON_COORDINATOR_URL=http://127.0.0.1:8766
AMAZON_COORDINATOR_CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
# Production coordinator should use PostgreSQL; local development defaults to SQLite when omitted.
# AMAZON_COORDINATOR_DATABASE_URL=postgresql+psycopg://ffp_crawler:change-me@127.0.0.1:5432/ffp_crawler
# Completed job results remain available to the UI temporarily, then are deleted automatically.
AMAZON_COORDINATOR_JOB_RETENTION_MINUTES=60
# Comma-separated HTTP(S) proxy URLs in the local .env.local file.
# AMAZON_CRAWLER_PROXIES=http://username:password@host:port,http://host2:port
# AMAZON_CRAWLER_PROXY_CONFIG=config/amazon-crawler-profiles.json

# --- Shopify Gateway Configuration ---
# Copy this file to .env.local and fill in your actual store credentials.
GATEWAY_STORE_ID=
GATEWAY_SHOP_DOMAIN=
GATEWAY_CLIENT_ID=
# GATEWAY_CLIENT_SECRET=

# Product streaming pipeline (server-side only)
SHOPIFY_PIPELINE_COORDINATOR_URL=http://127.0.0.1:8766
SHOPIFY_PIPELINE_WORKERS=4
SHOPIFY_PROXY_CONFIG=config/amazon-crawler-profiles.json
GATEWAY_HOST=127.0.0.1
GATEWAY_PORT=3001
# Set the same private value for coordinator and pipeline worker in production.
# SHOPIFY_PIPELINE_TOKEN=

# SEO pipeline (server-side only; Application Default Credentials are not committed)
# GOOGLE_CLOUD_PROJECT=
GOOGLE_CLOUD_LOCATION=global
GEMINI_ANALYSIS_MODEL=gemini-2.5-flash
SEO_SEARCH_PROVIDER=google
SEO_SEARCH_LANGUAGE=en
SEO_SEARCH_COUNTRY=us
SEO_EMBEDDING_PROVIDER=local
SEO_EMBEDDING_MODEL=text-embedding-004
SEO_CONFLICT_CORPUS_PATH=.runtime/seo-conflict-corpus.json

# Optional: Proxy Configuration (e.g. for US IP routing)
# GATEWAY_PROXY_URL=
# GATEWAY_PROXY_USERNAME=
# GATEWAY_PROXY_PASSWORD=

# Auto SEO Database Backup Configuration (Server-only SQLite)
AUTO_SEO_DB_PATH=.local-data/auto-seo.sqlite3
# Central Amazon product-image processing (runs after SEO and before Shopify).
IMAGE_PROCESSING_WORKERS=4
IMAGE_PROCESSING_CACHE_TTL_MINUTES=60
IMAGE_PROCESSING_CACHE_DIR=.runtime/image-processing
